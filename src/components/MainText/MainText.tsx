import React from 'react';
import {Text} from 'react-native';
import {styles} from './MainText.style';

const MainText = () => {
  return (
    <Text style={styles.mainText}>Press prototype to see button animation</Text>
  );
};

export default MainText;
